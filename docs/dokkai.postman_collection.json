{
  "info": {
    "name": "Dokkai API",
    "_postman_id": "d0kka1-0000-4000-8000-000000000001",
    "description": "HTTP API for Dokkai — a graph-aware vector database over a codebase.\n\nEvery route except Health, Auth → Login and Auth → Status requires a bearer token (see the Auth folder). Auth cannot be disabled.\n\nUsage order:\n1. Auth → Login (default admin/admin on first boot; captures {{authToken}} automatically and applies it to every other request via the collection's bearer auth).\n2. Config → Set LLM provider (or set OLLAMA_CHAT_MODEL on the server). Requires the admin role.\n3. Instances → Start pipeline (captures {{jobId}} automatically).\n4. Instances → Get job (poll until status = succeeded), or Job events (SSE) to stream progress.\n5. Chat → Chat (SSE): note Postman shows the raw event stream. Requires the admin or user role.\n6. Graph → List graphs / Get project graph / Get entity neighborhood / Get file dependency view, once a project is ingested.\n7. Routines → Launch review run (captures {{runId}}/{{jobId}} automatically), then Get routine run to poll for findings/summary once status is done. kind: bughunt currently 400s -- arrives in a later release.\n\nRoles: admin (everything, incl. user management and POST /config/llm), user (chat, ingestion, deletes, review-run launch/delete, all reads), viewer (read-only GETs only). A role that can't perform an action gets 403 \"role '<role>' cannot perform this action\".\n\nNote: only one pipeline/refresh/graph/review job may run per project at a time — a second one returns 409 (see the '409 - job already running' saved examples).\n\nCollection variables let you avoid editing every request: set {{baseUrl}}, {{repoPath}}, {{projectName}} once.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [{ "key": "token", "value": "{{authToken}}", "type": "string" }]
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:8000", "type": "string" },
    { "key": "repoPath", "value": "/absolute/path/to/your/repo", "type": "string" },
    { "key": "projectName", "value": "your-repo", "type": "string" },
    { "key": "jobId", "value": "", "type": "string" },
    { "key": "conversationId", "value": "", "type": "string" },
    { "key": "entityQualifiedName", "value": "src.alarms.AlarmValidationUseCase", "type": "string" },
    { "key": "authToken", "value": "", "type": "string" },
    { "key": "rootUsername", "value": "admin", "type": "string" },
    { "key": "rootPassword", "value": "admin", "type": "string" },
    { "key": "userId", "value": "", "type": "string" },
    { "key": "targetRef", "value": "feature/my-branch", "type": "string" },
    { "key": "baseRef", "value": "", "type": "string" },
    { "key": "runId", "value": "", "type": "string" }
  ],
  "item": [
    {
      "name": "Health",
      "item": [
        {
          "name": "Root (health check)",
          "request": {
            "auth": { "type": "noauth" },
            "method": "GET",
            "header": [],
            "url": { "raw": "{{baseUrl}}/", "host": ["{{baseUrl}}"], "path": [""] },
            "description": "Returns {\"status\":\"online\"} when the API is up. Public — no auth required."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Auth",
      "item": [
        {
          "name": "Login",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "if (res.token) {",
                  "  pm.collectionVariables.set('authToken', res.token);",
                  "  console.log('Saved authToken');",
                  "}",
                  "pm.test('login succeeded', function () {",
                  "  pm.expect(res).to.have.property('token');",
                  "});",
                  "if (res.default_admin_active) {",
                  "  console.warn('warning: the default admin/admin credentials are still active');",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "auth": { "type": "noauth" },
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"{{rootUsername}}\",\n  \"password\": \"{{rootPassword}}\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/auth/login",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "login"]
            },
            "description": "Public. Authenticate and receive a 30-day bearer token. The test script saves the token into {{authToken}}, which the collection's bearer auth applies to every other request. default_admin_active is true while the seeded admin/admin credentials are unchanged."
          },
          "response": [
            {
              "name": "401 - invalid credentials",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"username\": \"admin\",\n  \"password\": \"wrong\"\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/auth/login",
                  "host": ["{{baseUrl}}"],
                  "path": ["auth", "login"]
                }
              },
              "status": "Unauthorized",
              "code": 401,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"invalid username or password\"\n}"
            }
          ]
        },
        {
          "name": "Status",
          "request": {
            "auth": { "type": "noauth" },
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/auth/status",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "status"]
            },
            "description": "Public. { enabled: true, default_admin_active } -- whether the seeded admin/admin credentials are still active."
          },
          "response": []
        },
        {
          "name": "Me",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/auth/me",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "me"]
            },
            "description": "Requires any authenticated role. Current user's username, role, session expiry, default_admin_active."
          },
          "response": []
        },
        {
          "name": "List users",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/auth/users",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "users"]
            },
            "description": "Requires the admin role. Never includes password hashes."
          },
          "response": []
        },
        {
          "name": "Create user",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "if (res.id) {",
                  "  pm.collectionVariables.set('userId', res.id);",
                  "  console.log('Saved userId =', res.id);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"alice\",\n  \"password\": \"correct horse battery staple\",\n  \"role\": \"user\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/auth/users",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "users"]
            },
            "description": "Requires the admin role. role is one of admin|user|viewer. The test script saves the new user's id into {{userId}}."
          },
          "response": [
            {
              "name": "403 - insufficient role",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"username\": \"alice\",\n  \"password\": \"correct horse battery staple\",\n  \"role\": \"user\"\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/auth/users",
                  "host": ["{{baseUrl}}"],
                  "path": ["auth", "users"]
                }
              },
              "status": "Forbidden",
              "code": 403,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"role 'viewer' cannot perform this action\"\n}"
            }
          ]
        },
        {
          "name": "Delete user",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/auth/users/{{userId}}",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "users", "{{userId}}"]
            },
            "description": "Requires the admin role. 400 if you try to delete your own user or the last remaining admin."
          },
          "response": []
        },
        {
          "name": "Change own password",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"current_password\": \"{{rootPassword}}\",\n  \"new_password\": \"a new strong password\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/auth/users/me/password",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "users", "me", "password"]
            },
            "description": "Requires any authenticated role. 401 if current_password doesn't match. Revokes every OTHER session for this user; the session making this request stays valid."
          },
          "response": []
        },
        {
          "name": "Logout",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/auth/logout",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "logout"]
            },
            "description": "Requires any authenticated role. Invalidates the session (bearer token) used for this request."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Instances",
      "item": [
        {
          "name": "Start pipeline",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "if (res.job_id) {",
                  "  pm.collectionVariables.set('jobId', res.job_id);",
                  "  console.log('Saved jobId =', res.job_id);",
                  "}",
                  "pm.test('job queued', function () {",
                  "  pm.expect(res).to.have.property('job_id');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"repo_path\": \"{{repoPath}}\",\n  \"recreate\": false,\n  \"describe\": true\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/instances/pipeline",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "pipeline"]
            },
            "description": "Requires the admin or user role. Enqueue the ingestion pipeline. Returns { job_id, status } immediately and runs in the background.\n\nSet recreate:true only once, to migrate the schema (it drops the whole collection). The test script saves job_id into {{jobId}}.\n\ndescribe defaults to true. Set describe:false to opt out of per-entity LLM descriptions for this run -- this also skips the descriptor pre-flight below, and the summary named vector stays empty for this run's chunks.\n\nFail-loud descriptor policy (describe:true only): the configured DESC_MODEL/DESC_PROVIDER is checked before the job is created. Missing or unavailable descriptor -> 400, no job created (see the '400 - no descriptor model' saved example).\n\nPer-project job lock: a second pipeline/refresh/graph-only job for a project that already has one queued/running -> 409, no job created (see the '409 - job already running' saved example)."
          },
          "response": [
            {
              "name": "401 - missing or invalid token",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"repo_path\": \"{{repoPath}}\",\n  \"recreate\": false\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/instances/pipeline",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "pipeline"]
                }
              },
              "status": "Unauthorized",
              "code": 401,
              "_postman_previewlanguage": "json",
              "header": [
                { "key": "Content-Type", "value": "application/json" },
                { "key": "WWW-Authenticate", "value": "Bearer" }
              ],
              "cookie": [],
              "body": "{\n  \"detail\": \"missing bearer token\"\n}"
            },
            {
              "name": "403 - insufficient role (viewer)",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"repo_path\": \"{{repoPath}}\",\n  \"recreate\": false\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/instances/pipeline",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "pipeline"]
                }
              },
              "status": "Forbidden",
              "code": 403,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"role 'viewer' cannot perform this action\"\n}"
            },
            {
              "name": "400 - no descriptor model",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"repo_path\": \"{{repoPath}}\",\n  \"recreate\": false\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/instances/pipeline",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "pipeline"]
                }
              },
              "status": "Bad Request",
              "code": 400,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"no descriptor model specified — set DESC_MODEL (e.g. qwen2.5-coder:3b) or configure a remote provider via DESC_PROVIDER\"\n}"
            },
            {
              "name": "409 - job already running",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"repo_path\": \"{{repoPath}}\",\n  \"recreate\": false\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/instances/pipeline",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "pipeline"]
                }
              },
              "status": "Conflict",
              "code": 409,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"another job is running for project 'your-repo'\"\n}"
            }
          ]
        },
        {
          "name": "Start pipeline (recreate — schema migration)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"repo_path\": \"{{repoPath}}\",\n  \"recreate\": true\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/instances/pipeline",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "pipeline"]
            },
            "description": "DANGER: recreate:true drops the ENTIRE collection (all projects) before inserting. Use only once, after a schema change."
          },
          "response": []
        },
        {
          "name": "Start pipeline (no descriptions)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"repo_path\": \"{{repoPath}}\",\n  \"recreate\": false,\n  \"describe\": false\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/instances/pipeline",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "pipeline"]
            },
            "description": "describe:false opts out of per-entity LLM descriptions for this run. Skips the descriptor pre-flight (no 400 even if DESC_MODEL is unset/unpulled) and ingests without descriptions -- the summary named vector stays empty for this project's chunks (equivalent to `dokkai ingest --no-describe`)."
          },
          "response": []
        },
        {
          "name": "Start graph-only run",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "if (res.job_id) {",
                  "  pm.collectionVariables.set('jobId', res.job_id);",
                  "  console.log('Saved jobId =', res.job_id);",
                  "}",
                  "pm.test('job queued', function () {",
                  "  pm.expect(res).to.have.property('job_id');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"repo_path\": \"{{repoPath}}\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/instances/graph",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "graph"]
            },
            "description": "Requires the admin or user role. Enqueue a graph-ONLY run (cgr only -- no chunking, no LLM descriptions, no embedding, no Weaviate). Returns { job_id, status } immediately; runs in the background with stages cgr -> done and kind: \"graph\". result.ingest.output_json is the same canonical graph JSON path (ingested/<project>.json) the full pipeline produces, readable by the Graph API as soon as the job succeeds. Shares the same per-project job lock as Start pipeline / Refresh descriptions. The test script saves job_id into {{jobId}}."
          },
          "response": [
            {
              "name": "409 - job already running",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"repo_path\": \"{{repoPath}}\"\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/instances/graph",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "graph"]
                }
              },
              "status": "Conflict",
              "code": 409,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"another job is running for project 'your-repo'\"\n}"
            }
          ]
        },
        {
          "name": "List jobs",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/instances/jobs",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "jobs"]
            },
            "description": "List all ingestion jobs, most recent first. In-memory; resets on server restart."
          },
          "response": []
        },
        {
          "name": "Get job",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/instances/jobs/{{jobId}}",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "jobs", "{{jobId}}"]
            },
            "description": "Poll a job's progress. Watch stage (cgr -> chunk -> describe -> upsert -> done for pipeline jobs, chunk -> describe -> update -> done for refresh jobs, cgr -> done for graph-only jobs), stage_progress {stage,done,total}, kind (pipeline|refresh|graph), and result.vectorize.descriptions (pipeline) or result.descriptions (refresh) -- graph-only jobs' result has only ingest.output_json."
          },
          "response": []
        },
        {
          "name": "Job events (SSE)",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "text/event-stream" }],
            "url": {
              "raw": "{{baseUrl}}/instances/jobs/{{jobId}}/events",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "jobs", "{{jobId}}", "events"]
            },
            "description": "Streams the job's progress as Server-Sent Events, mirroring Chat (SSE)'s framing. Emits the current state on connect, then again whenever updated_at changes (~0.5s poll). event: job for in-progress updates, event: done for the terminal state (status = succeeded|failed), after which the stream closes. Each event's data is the same Job object as GET /instances/jobs/{id}. Unknown job_id -> plain 404 (not a stream).\n\nNote: Postman buffers the stream and shows it after completion; use curl -N for live events."
          },
          "response": []
        },
        {
          "name": "Refresh descriptions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "if (res.job_id) {",
                  "  pm.collectionVariables.set('jobId', res.job_id);",
                  "  console.log('Saved jobId =', res.job_id);",
                  "}",
                  "pm.test('job queued', function () {",
                  "  pm.expect(res).to.have.property('job_id');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{baseUrl}}/instances/{{projectName}}/describe",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "{{projectName}}", "describe"]
            },
            "description": "Requires the admin or user role. Re-run only the describe pass over an already-ingested project (background job; no cgr, no re-embedding of unchanged text). Cache is honored (default force:false) -- unchanged entities cost nothing. Same fail-loud descriptor pre-flight as the pipeline, plus 404 if the project has no chunks and 409 if its graph JSON is missing from ingested/. The test script saves job_id into {{jobId}}."
          },
          "response": [
            {
              "name": "404 - project has no chunks",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": { "mode": "raw", "raw": "{}" },
                "url": {
                  "raw": "{{baseUrl}}/instances/{{projectName}}/describe",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "{{projectName}}", "describe"]
                }
              },
              "status": "Not Found",
              "code": 404,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"project 'your-repo' has no chunks — ingest it first with POST /instances/pipeline\"\n}"
            },
            {
              "name": "409 - no graph JSON found",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": { "mode": "raw", "raw": "{}" },
                "url": {
                  "raw": "{{baseUrl}}/instances/{{projectName}}/describe",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "{{projectName}}", "describe"]
                }
              },
              "status": "Conflict",
              "code": 409,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"no graph JSON found for project 'your-repo' in ingested/ — re-run POST /instances/pipeline to regenerate it\"\n}"
            },
            {
              "name": "409 - job already running",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": { "mode": "raw", "raw": "{}" },
                "url": {
                  "raw": "{{baseUrl}}/instances/{{projectName}}/describe",
                  "host": ["{{baseUrl}}"],
                  "path": ["instances", "{{projectName}}", "describe"]
                }
              },
              "status": "Conflict",
              "code": 409,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"another job is running for project 'your-repo'\"\n}"
            }
          ]
        },
        {
          "name": "Refresh descriptions (force)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"force\": true\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/instances/{{projectName}}/describe",
              "host": ["{{baseUrl}}"],
              "path": ["instances", "{{projectName}}", "describe"]
            },
            "description": "Same as 'Refresh descriptions' but bypasses the description cache -- every eligible entity is regenerated, even if its source hasn't changed."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Chat",
      "item": [
        {
          "name": "Chat (SSE)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// The response is an SSE stream. Postman shows it raw.",
                  "// Extract conversation_id from the final 'done' event if present.",
                  "const body = pm.response.text();",
                  "const m = body.match(/\\\"conversation_id\\\":\\s*\\\"([^\\\"]+)\\\"/);",
                  "if (m) {",
                  "  pm.collectionVariables.set('conversationId', m[1]);",
                  "  console.log('Saved conversationId =', m[1]);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Accept", "value": "text/event-stream" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"message\": \"how does the alarm flow work?\",\n  \"project_name\": \"{{projectName}}\",\n  \"audience\": \"developer\",\n  \"top_k\": 8\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/chat",
              "host": ["{{baseUrl}}"],
              "path": ["chat"]
            },
            "description": "Requires the admin or user role. Streams a Server-Sent Events response: sources → token* → done (or error). project_name must match an ingested repo. audience ∈ developer|manager|customer.\n\nEach entry in the 'sources' event's data array includes both file_path (relative) and absolute_path (full path on disk, as persisted at ingest time).\n\nNote: Postman buffers the stream and shows it after completion; use curl -N for live tokens."
          },
          "response": []
        },
        {
          "name": "Chat (continue conversation)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Accept", "value": "text/event-stream" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"message\": \"and where are the false positives handled?\",\n  \"project_name\": \"{{projectName}}\",\n  \"audience\": \"developer\",\n  \"conversation_id\": \"{{conversationId}}\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/chat",
              "host": ["{{baseUrl}}"],
              "path": ["chat"]
            },
            "description": "Follow-up in the same conversation, using {{conversationId}} captured by the previous request."
          },
          "response": []
        },
        {
          "name": "List conversations",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/chat/conversations",
              "host": ["{{baseUrl}}"],
              "path": ["chat", "conversations"]
            },
            "description": "List stored conversations (persisted in Postgres). Includes title (derived from the first user message, null for older conversations) and audience."
          },
          "response": []
        },
        {
          "name": "Get conversation",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/chat/conversations/{{conversationId}}",
              "host": ["{{baseUrl}}"],
              "path": ["chat", "conversations", "{{conversationId}}"]
            },
            "description": "Full history (messages + their sources) for one conversation."
          },
          "response": []
        },
        {
          "name": "Delete conversation",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/chat/conversations/{{conversationId}}",
              "host": ["{{baseUrl}}"],
              "path": ["chat", "conversations", "{{conversationId}}"]
            },
            "description": "Requires the admin or user role. Delete a conversation and its history."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Graph",
      "item": [
        {
          "name": "List graphs",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/graph",
              "host": ["{{baseUrl}}"],
              "path": ["graph"]
            },
            "description": "List every project with an ingested graph in ingested/. Includes chunks (Weaviate chunk count, null if Weaviate is unreachable) and repo_path (derived from a File node, null if underivable). Returns [] when nothing has been ingested yet. Restart-proof -- read straight off the filesystem."
          },
          "response": []
        },
        {
          "name": "Get project graph (code entities)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/graph/{{projectName}}?limit=5000",
              "host": ["{{baseUrl}}"],
              "path": ["graph", "{{projectName}}"],
              "query": [{ "key": "limit", "value": "5000" }]
            },
            "description": "Normalized graph for the project: code-entity nodes (Class/Function/Method/Interface/Enum/Type) and edges among them only. 404 if the project was never ingested."
          },
          "response": []
        },
        {
          "name": "Get project graph (structural)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/graph/{{projectName}}?include=structural&limit=5000",
              "host": ["{{baseUrl}}"],
              "path": ["graph", "{{projectName}}"],
              "query": [
                { "key": "include", "value": "structural" },
                { "key": "limit", "value": "5000" }
              ]
            },
            "description": "Every node (code entities, folders, files, modules, packages, the project, external packages) and every edge, up to limit."
          },
          "response": []
        },
        {
          "name": "Get entity",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/graph/{{projectName}}/entity?name={{entityQualifiedName}}",
              "host": ["{{baseUrl}}"],
              "path": ["graph", "{{projectName}}", "entity"],
              "query": [{ "key": "name", "value": "{{entityQualifiedName}}" }]
            },
            "description": "Resolved graph node for name (same resolution as the neighborhood endpoint's entity param), enriched with description and chunk_text fetched from Weaviate via the same deterministic-UUID lookup the MCP get_entity tool uses. description/chunk_text are null (not an error) when the entity has no chunk in Weaviate or Weaviate is unreachable. 404 if the project has no graph, or if the entity isn't in it."
          },
          "response": []
        },
        {
          "name": "Get entity neighborhood",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/graph/{{projectName}}/neighborhood?entity={{entityQualifiedName}}&depth=1&direction=both&limit=200",
              "host": ["{{baseUrl}}"],
              "path": ["graph", "{{projectName}}", "neighborhood"],
              "query": [
                { "key": "entity", "value": "{{entityQualifiedName}}" },
                { "key": "depth", "value": "1" },
                { "key": "direction", "value": "both" },
                { "key": "limit", "value": "200" }
              ]
            },
            "description": "BFS over CALLS/INHERITS/IMPLEMENTS/OVERRIDES/DEFINES/DEFINES_METHOD edges only (never IMPORTS or containment/external edges). Nodes carry hop (0 = center); the center is also returned separately. 404 if the project has no graph, or if entity isn't in it."
          },
          "response": []
        },
        {
          "name": "Get file dependency view",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/graph/{{projectName}}/files",
              "host": ["{{baseUrl}}"],
              "path": ["graph", "{{projectName}}", "files"]
            },
            "description": "Directed file-to-file dependency aggregation, internal files only (external packages/modules excluded, self-loops skipped). Each edge has a total weight and a per relationship-type types breakdown."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Routines",
      "item": [
        {
          "name": "Launch review run",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const res = pm.response.json();",
                  "if (res.run_id) {",
                  "  pm.collectionVariables.set('runId', res.run_id);",
                  "  console.log('Saved runId =', res.run_id);",
                  "}",
                  "if (res.job_id) {",
                  "  pm.collectionVariables.set('jobId', res.job_id);",
                  "  console.log('Saved jobId =', res.job_id);",
                  "}",
                  "pm.test('run launched', function () {",
                  "  pm.expect(res).to.have.property('run_id');",
                  "  pm.expect(res).to.have.property('job_id');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"kind\": \"review\",\n  \"project\": \"{{projectName}}\",\n  \"target_ref\": \"{{targetRef}}\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/routines/runs",
              "host": ["{{baseUrl}}"],
              "path": ["routines", "runs"]
            },
            "description": "Requires the admin or user role. Launches a code review run as a background job (kind: bughunt 400s -- arrives in a later release). Returns 202 { run_id, job_id } immediately.\n\nbase_ref defaults to the project's default base branch (origin/HEAD, else local main/master) when omitted. model/provider (both optional) override the active LLM config for this run only.\n\nModel resolution -- no hardcoded default: model/provider in the body, else the active POST /config/llm config; if neither resolves to a usable provider+model, 400 with the 'no LLM configured' message (see the saved example).\n\nPre-flights (in order, before any job is created): project must have an ingested graph (404), repo_path must be a git repo (400), base_ref/target_ref must resolve (400), the resolved LLM must be healthy (400).\n\nShares the same per-project job lock as /instances/pipeline, /instances/graph and /instances/{project}/describe -- 409 if a job is already running for this project (see the '409 - job already running' saved example). The test script saves run_id into {{runId}} and job_id into {{jobId}}."
          },
          "response": [
            {
              "name": "400 - no LLM configured",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"kind\": \"review\",\n  \"project\": \"{{projectName}}\",\n  \"target_ref\": \"{{targetRef}}\"\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/routines/runs",
                  "host": ["{{baseUrl}}"],
                  "path": ["routines", "runs"]
                }
              },
              "status": "Bad Request",
              "code": 400,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"No LLM provider configured for this review. POST to /config/llm to set one up, or pass model/provider in the launch payload.\"\n}"
            },
            {
              "name": "400 - bughunt not yet implemented",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"kind\": \"bughunt\",\n  \"project\": \"{{projectName}}\"\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/routines/runs",
                  "host": ["{{baseUrl}}"],
                  "path": ["routines", "runs"]
                }
              },
              "status": "Bad Request",
              "code": 400,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"bug hunt routines arrive in a later release\"\n}"
            },
            {
              "name": "404 - project not ingested",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"kind\": \"review\",\n  \"project\": \"{{projectName}}\",\n  \"target_ref\": \"{{targetRef}}\"\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/routines/runs",
                  "host": ["{{baseUrl}}"],
                  "path": ["routines", "runs"]
                }
              },
              "status": "Not Found",
              "code": 404,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"no graph found for project 'your-repo' — run POST /instances/pipeline to ingest it\"\n}"
            },
            {
              "name": "409 - job already running",
              "originalRequest": {
                "method": "POST",
                "header": [{ "key": "Content-Type", "value": "application/json" }],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"kind\": \"review\",\n  \"project\": \"{{projectName}}\",\n  \"target_ref\": \"{{targetRef}}\"\n}"
                },
                "url": {
                  "raw": "{{baseUrl}}/routines/runs",
                  "host": ["{{baseUrl}}"],
                  "path": ["routines", "runs"]
                }
              },
              "status": "Conflict",
              "code": 409,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"another job is running for project 'your-repo'\"\n}"
            }
          ]
        },
        {
          "name": "Launch review run (explicit base_ref + model override)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"kind\": \"review\",\n  \"project\": \"{{projectName}}\",\n  \"target_ref\": \"{{targetRef}}\",\n  \"base_ref\": \"{{baseRef}}\",\n  \"model\": \"qwen2.5-coder:14b\",\n  \"provider\": \"ollama\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/routines/runs",
              "host": ["{{baseUrl}}"],
              "path": ["routines", "runs"]
            },
            "description": "Same as Launch review run, but with an explicit base_ref and a per-run model/provider override -- these bypass the active POST /config/llm config for just this run. qwen2.5-coder:14b is the recommended local model for review (measured: 3b-class models are not reliable for review judgment)."
          },
          "response": []
        },
        {
          "name": "List routine runs",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/routines/runs?project={{projectName}}",
              "host": ["{{baseUrl}}"],
              "path": ["routines", "runs"],
              "query": [{ "key": "project", "value": "{{projectName}}" }]
            },
            "description": "Requires any authenticated role. Most recent first, optionally filtered by project and/or kind (query params, both optional; limit defaults to 50, max 200). Each entry carries severity_counts (severity -> count over that run's findings)."
          },
          "response": []
        },
        {
          "name": "Get routine run",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/routines/runs/{{runId}}",
              "host": ["{{baseUrl}}"],
              "path": ["routines", "runs", "{{runId}}"]
            },
            "description": "Requires any authenticated role. The run's full detail, findings included. Poll this (or stream GET /instances/jobs/{{jobId}}/events) until status is done or failed; summary/stats/findings fill in once terminal."
          },
          "response": [
            {
              "name": "404 - run not found",
              "originalRequest": {
                "method": "GET",
                "header": [],
                "url": {
                  "raw": "{{baseUrl}}/routines/runs/{{runId}}",
                  "host": ["{{baseUrl}}"],
                  "path": ["routines", "runs", "{{runId}}"]
                }
              },
              "status": "Not Found",
              "code": 404,
              "_postman_previewlanguage": "json",
              "header": [{ "key": "Content-Type", "value": "application/json" }],
              "cookie": [],
              "body": "{\n  \"detail\": \"routine run '<run_id>' not found\"\n}"
            }
          ]
        },
        {
          "name": "Delete routine run",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/routines/runs/{{runId}}",
              "host": ["{{baseUrl}}"],
              "path": ["routines", "runs", "{{runId}}"]
            },
            "description": "Requires the admin or user role. Deletes the run, cascading to its findings (404 if the run doesn't exist)."
          },
          "response": []
        },
        {
          "name": "List git branches",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/routines/git/branches?project={{projectName}}",
              "host": ["{{baseUrl}}"],
              "path": ["routines", "git", "branches"],
              "query": [{ "key": "project", "value": "{{projectName}}" }]
            },
            "description": "Requires any authenticated role. Local branches of the project's repository (current branch and most-recently-committed first) plus the resolved default_base. 404 if the project has no ingested graph; 400 if repo_path isn't a git repo or no default base branch could be determined."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Config",
      "item": [
        {
          "name": "Set LLM config (Ollama, local)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"is_local\": true,\n  \"provider_data\": {\n    \"provider_name\": \"ollama\",\n    \"model\": \"qwen2.5-coder:latest\"\n  }\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/config/llm",
              "host": ["{{baseUrl}}"],
              "path": ["config", "llm"]
            },
            "description": "Requires the admin role. Configure a local Ollama chat model. Validates that the model is installed."
          },
          "response": []
        },
        {
          "name": "Set LLM config (OpenAI, remote)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"is_local\": false,\n  \"provider_data\": {\n    \"provider_name\": \"openai\",\n    \"model\": \"gpt-4o-mini\",\n    \"key\": \"sk-...\"\n  }\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/config/llm",
              "host": ["{{baseUrl}}"],
              "path": ["config", "llm"]
            },
            "description": "Requires the admin role. Configure a remote provider (openai|anthropic). Requires provider_data.key. The exact model is probed at save time (models.retrieve) -- an invalid/retired model is rejected with 400 and nothing is saved."
          },
          "response": []
        },
        {
          "name": "Get LLM config",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/config/llm",
              "host": ["{{baseUrl}}"],
              "path": ["config", "llm"]
            },
            "description": "Current active LLM configuration (404 if none set)."
          },
          "response": []
        },
        {
          "name": "List models",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/config/llm/models",
              "host": ["{{baseUrl}}"],
              "path": ["config", "llm", "models"]
            },
            "description": "Models advertised by the active provider. openai/anthropic fetch the provider's live catalog with a short timeout, falling back to a static list on any failure; ollama lists what's pulled locally."
          },
          "response": []
        },
        {
          "name": "Provider health",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/config/llm/health",
              "host": ["{{baseUrl}}"],
              "path": ["config", "llm", "health"]
            },
            "description": "Connectivity check for the active provider -- probes the currently configured model (not a hard-coded one): remote providers call models.retrieve(model), ollama checks /api/tags."
          },
          "response": []
        }
      ]
    }
  ]
}
