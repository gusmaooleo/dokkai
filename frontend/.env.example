# Base URL of the Dokkai API. Must be prefixed NEXT_PUBLIC_ to be bundled
# into the browser JS (see frontend/lib/api.ts) — inlined at build time,
# so set it before `next build` in each environment you deploy to.
NEXT_PUBLIC_API_URL=http://localhost:8000
